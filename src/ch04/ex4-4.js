var todos = [
    "Pick up drycleaning",
    "Clean Batcave",
    "Save Gotham",
];
for (var index in todos) {
    console.log(index + ": " + todos[index]);
}
for (var _i = 0, todos_1 = todos; _i < todos_1.length; _i++) {
    var value = todos_1[_i];
    console.log(value);
}
